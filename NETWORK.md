# NETWORK.md — SkyLens 네트워크 아키텍처

> 2026-08-03 회의 "할 것" 2번(KOREN egress), 3번(무선/광케이블), 4번(통신 스펙·nginx·미들웨어) 담당 산출물.
> ARCHITECTURE.md의 4-Tier 설계를 전제로 하고, 그 문서가 비워둔 전송 계층의 구체 스펙을 채운다.

---

## 0. 설계가 만족해야 하는 요구사항 (기존 문서에서 도출)

| 요구사항 | 출처 |
|---|---|
| 첫 화면(버전 1 복원)까지 최대 30초, 이후 30초 주기 점진 갱신 | 회의록, ARCHITECTURE.md §6 |
| 드론 2~3대의 영상(RGB+열화상)+포즈 스트림 동시 업링크 | IDEA.md |
| 실험군(KOREN) vs 대조군(일반망) — **같은 코드, 두 경로** 대조 측정 | ARCHITECTURE.md 설계 원칙 ③ |
| 장면 1회 갱신 페이로드 = `.ply` 13~30 MB (실측) | recon/bollard 실측 |
| 재난 현장 가정 → 현장 유선(광케이블) 불가, 무선만 | 회의록 3번 |
| 스트림 수신은 MediaMTX, 관제는 WebSocket, 텔레메트리는 MAVLink | ARCHITECTURE.md, RESEARCH-GEO3D.md §16 |

핵심 전제 하나를 명시한다: **30초는 "AI까지 끝나는 시간"이 아니라 "화면이 갱신되는 주기"다.**
AI 탐지는 병렬 스트림으로 돌고 발견 시마다 커밋(마커 push)한다 — 회의록의 "AI는 파이프라인에서 별도로 동작" 결정과 일치. 따라서 네트워크가 30초 예산 안에 책임질 것은 ①업링크 ②복원 잡 1회 ③산출물 배포, 세 가지다.

---

## 1. 전체 아키텍처 (확정본, 2026-08-04 — 사무국 회신 반영)

```
┌─ 현장 (재난 지역: 무선 only) ──────────────────────────────┐
│  드론×2~3 (RGB+열화상)                                      │
│    │ 제조사 무선링크 (OcuSync 등 — 우리가 못 바꾸는 구간)     │
│    ▼                                                       │
│  현장 게이트웨이(GW) = KOREN이 말한 "망외 에지 서버"          │
│    · 영상 수신 → H.265 재인코딩 → SRT 스트림 생성            │
│    · 포즈(MAVLink) 타임스탬프 결합, NTP/GPS 타임싱크         │
│    · 로컬 WiFi AP: 출동대원 폴백 서빙 (백홀 단절 대비)        │
└──────────────────────┬─────────────────────────────────────┘
                       │ 상용 5G 위에 SRT(UDP) + WSS(관제)
                       │   개발·중간평가: 일반 5G (CGNAT, GW가 발신)
                       │   최종평가 후보: 고정 IP M2M 라우터 직결*
                       │              또는 이음 특화망 5G-A(판교) 직결*
                       ▼
┌─ 브리지: 충남대 서버 168.188.128.36 (공대 5호관) ───────────┐
│  KOREN 등록 IP (자원 신청 폼 7번 제출 완료)                  │
│  · 방화벽 개방(폼 8번): UDP 8890 추가 신청 필요**            │
│  · 실험군: ──> KOREN ──> 망내 자원                          │
│  · 대조군: ──> 일반 인터넷 ──> 상용 클라우드 (재설계 검토 중, │
│                §3.6 갭 3 — 폐쇄망이라 동일 목적지 이중 경로   │
│                불가, 동일 파이프라인 병행 배포안)             │
└──────────────────────┬─────────────────────────────────────┘
                       ▼
┌─ Core (KOREN 망내, 자원 신청 폼으로 위치 확정) ─────────────┐
│  KOREN VM(대전): MediaMTX(SRT 수신) + FastAPI/Redis         │
│                  오케스트레이터 · 충남대에서 지리적 최단      │
│  HPC V100(판교) 또는 AI Cloud GPU(H200):                    │
│    3DGS 증분 복원 잡(30초 주기) ∥ UNet 추론(병렬, 발견 시     │
│    커밋) → Depth 레이캐스팅(1회)                             │
│  → Object Storage(.ply/.spz+마커) · 대전↔판교 = KOREN 백본  │
└──────────────────────┬─────────────────────────────────────┘
                       │ push (30초 주기, 13~30 MB/장면)
                       ▼
┌─ Edge (배포 지점) ─────────────────────────────────────────┐
│  nginx: TLS 종단 · 정적 .ply/.spz 서빙 · WSS 프록시          │
│  위치: 충남대 168.188.128.36 (권장) ***                      │
│        NIA PoP VM은 임의 인바운드 승인 시 승격 (§3.7)         │
└───────┬──────────────────────────────┬─────────────────────┘
        ▼ HTTPS/WSS                    ▼ HTTPS/WSS (5G)
   상황판(관제탑, 왼쪽 화면)        현장 출동대원 뷰어(오른쪽 화면)
                                       └ 폴백: GW WiFi AP
```

\* 두 직결 옵션 모두 사전 화상회의에서 확답 필요 — M2M 라우터는 무선 고정 IP의 등록 가능 여부, 이음 5G-A는 시연 활용 가능 여부 (§3.6).
\*\* 현재 방화벽 신청분은 22/80/443뿐이라 영상 스트림이 차단 상태. 폼 8번 수정이 최우선 액션 (§3.6 갭 1).
\*\*\* Edge를 NIA PoP VM에 두면 CGNAT 단말(대원 폰·심사위원)이 접속할 수 없다 — KOREN은 등록 IP만 인바운드를 받기 때문. 배포 지점은 KOREN과 인터넷에 동시에 발을 걸친 충남대 서버여야 한다 (§3.7).

구조에서 나오는 가장 중요한 통찰 세 가지:

1. **무선 액세스 구간(드론→GW→5G)은 실험군/대조군 공통이고, KOREN vs 일반망 대조는 백본 구간(브리지→Core HPC→Edge)에서 이루어진다.** 무선 구간 변수를 공통으로 묶어야 백본 차이만 측정된다.
2. **KOREN 진입은 기술 문제가 아니라 행정 절차다** — 고정 IP를 KOREN에 등록하면 접속 자격이 생긴다(사무국 확인). 홀펀칭·NAT 통과 구현은 불필요해졌고, 남은 것은 고정 IP 확보(충남대 서버)와 등록 요청뿐이다.
3. **KOREN이 가속하는 것은 드론 영상이 아니라 3D 모델이다.** 인제스트 총량은 상용 5G(15~35 Mbps)가 결정하므로 KOREN 백본으로 늘릴 수 없다. 백본이 대체 불가능한 지점은 30초마다 터지는 13~30 MB 모델 버스트를 수백 ms 안에 배포하는 구간이다 (§2.5).

---

## 2. 현장 액세스 구간 — 할 것 3번 (와이파이/광케이블)

### 2.1 왜 전 구간 무선인가

재난 현장(화재·붕괴)에서는 기존 유선 인프라가 파괴됐거나 접근 불가라고 가정한다. 광케이블 신규 포설은 시간상 불가능하다. 따라서 현장 구간은 전부 무선이고, 유선(KOREN 포함)은 브리지 지점부터 시작한다. 이 논리는 발표 때 "왜 KOREN 회선을 현장에 안 끌었나"라는 질문의 방어 논리로도 쓴다.

### 2.2 드론 → 현장 게이트웨이: 두 가지 옵션

| | 옵션 A (권장, MVP) | 옵션 B (확장) |
|---|---|---|
| 구성 | 드론 제조사 무선링크(OcuSync 등)로 GW 노트북에 영상 수신 → GW가 SRT 인코딩 | 드론에 초소형 LTE/5G 인코더 탑재, 드론이 직접 업링크 |
| 근거 사례 | 일반적 드론 운용 방식 | SKT T Live Caster: 1080p60 H.265, 140g, LTE로 지연 약 1초 (netmanias 재난구조 시스템 사례) |
| 장점 | 추가 하드웨어 없음, 드론 대수 확장 시 GW만 증설 | GW 없이 드론별 독립 업링크, 가시선 제약 없음 |
| 단점 | 제조사 링크 가시선·거리 제약(수 km) | 대당 수백만 원, 무게·배터리 부담 |

MVP는 옵션 A로 간다. GW는 노트북 1대 + 상용 5G 라우터로 구성하고, 여기서 **포즈(MAVLink 텔레메트리)를 영상 타임스탬프에 결합**한다. 포즈가 붙어야 SfM 90% 병목(실측 36분)을 건너뛰고 30초 주기가 성립하므로, GW의 타임싱크(NTP/PTP)가 좌표 동기화 외에 추가로 필요한 동기화다 — 회의록의 "좌표 동기화 말고도 추가 동기화" 항목이 바로 이것.

### 2.3 업링크 대역폭 산정

| 스트림 | 코덱/설정 | 대당 비트레이트 | 3대 합계 |
|---|---|---|---|
| RGB 4K 30fps | H.265 실시간 | 15~25 Mbps | 45~75 Mbps |
| RGB 1080p 30fps (폴백) | H.265 실시간 | 4~8 Mbps | 12~24 Mbps |
| 열화상 640×512 | H.265 | 1~3 Mbps | 3~9 Mbps |
| 포즈/텔레메트리 | MAVLink | ~50 kbps | 무시 가능 |

상용 5G 업링크 실효치는 셀 상황에 따라 20~60 Mbps 수준이다. 즉 **4K×3대는 단일 5G 회선에서 성립하지 않거나 아슬아슬하다.** 대응은 두 겹:

1. **적응 인코딩**: GW에서 링크 상태에 따라 4K→1080p 다운스케일. 복원 입력 실측이 긴 변 1600px였으므로 1080p로도 3DGS 품질은 유지된다 — 4K 원본은 GW에 녹화해 뒀다가 사후 고품질 재복원에 쓴다(회의록의 "Record Only Transform" 방향과 일치).
2. **이 병목 자체가 대조군 논거**: "일반 5G/LTE로는 다중 드론 스트림이 병목"이라는 IDEA.md의 주장을 여기서 실측으로 증명한다.

### 2.4 WiFi의 역할

WiFi는 백홀이 아니라 **현장 로컬 구간**에만 쓴다: GW가 AP를 띄워 현장 출동대원 단말이 접속 → Edge VM까지 나갔다 오지 않고 GW 캐시에서 최신 복원본을 받는 로컬 폴백 경로(백홀 단절 시 생존성). 평시에는 대원 단말도 5G로 Edge VM에 직접 붙는다.

### 2.5 "10Gbps급 고속망" 주장의 재정의 — 병목은 액세스망이고, 백본이 필요한 건 영상이 아니라 모델이다

제안서 초안에 "고해상도 RGB+열화상을 10Gbps급 고속망으로 실시간 전송 / 드론 1대당 50~100Mbps, 3대 수백 Mbps 동시 전송"이라고 쓰여 있었다. 이 문장은 그대로 두면 심사에서 깨진다. 세 가지가 겹쳐 있어서 분리해야 한다.

**(1) 대당 50~100Mbps는 과대 산정이다.** §2.3 실측 기준으로 H.265 실시간 인코딩은 4K30이 15~25 Mbps, 1080p30이 4~8 Mbps다. 50~100 Mbps는 4K60 고비트레이트나 준무압축 가정에서 나오는 값이다. 그리고 복원 입력 실측이 긴 변 1600px였으므로 **실시간 경로는 1080p로 운용하고 4K 원본은 GW 녹화 후 사후 재복원**한다는 결정이 이미 §2.3에 있다. 실제 실시간 소요는 다음과 같다.

| 운용 모드 | 3대 합계 | 상용 5G 업링크(실효 20~60 Mbps)에서 |
|---|---|---|
| 4K30 + 열화상 | 48~84 Mbps | 아슬아슬하거나 불가 |
| **1080p30 + 열화상 (기본 운용)** | **15~33 Mbps** | **성립** |

**(2) 앞단이 100 Mbps면 뒷단이 40G여도 총량은 100 Mbps다 — 이 지적은 옳다.** 드론이 KOREN을 직접 타지 않는 이상 액세스 구간(드론→GW→상용 5G)이 전체 인제스트 대역을 결정한다. 따라서 **KOREN을 "업링크 가속기"로 설명하면 안 된다.** 이 병목은 숨기는 게 아니라 실측해서 대조군 논거로 쓴다(§2.3의 2번 항목).

**(3) 10Gbps가 실제로 필요한 구간은 따로 있다 — 3DGS 모델 배포다.** 제안서의 "Core↔Edge 3D 모델 전송 저지연(수백 ms 이내)은 전용망만 충족"이라는 문장은 정확하다. 대상이 영상이 아니라 모델일 뿐이다.

장면 1회 갱신 페이로드 30 MB = 240 Mbit를 링크에 밀어 넣는 시간:

| 링크 대역 | 전송 시간 | 판정 |
|---|---|---|
| 100 Mbps | 2.4 s | 30초 예산의 8%를 배포에만 소모 |
| 1 Gbps | 240 ms | "수백 ms 이내" 충족 |
| 10 Gbps | 24 ms | 여유 |

이건 30초마다 터지는 **버스트**라 평균 대역(30 MB/30 s ≈ 8 Mbps)이 아니라 순간 대역이 지배한다. 상용 인터넷 경로에서는 여기에 경합·TCP slow start가 얹혀 실측은 더 나빠진다. 즉 §4.3 예산표의 "Core → Edge push < 1 s"는 Gbps급 백본을 전제할 때만 성립하며, 이것이 **KOREN이 대체 불가능한 유일한 구간**이다.

**정정된 주장 문구:**

| 초안 | 정정 |
|---|---|
| 고해상도 RGB+열화상을 10Gbps급 고속망으로 실시간 전송 | 드론 영상 인제스트는 상용 5G(15~35 Mbps)로 수집한다 — 이 구간은 실험군/대조군 공통 변수다 |
| 드론 1대당 50~100Mbps, 3대 수백 Mbps 동시 전송 필요 | 드론 1대당 5~11 Mbps(1080p+열화상), 3대 15~35 Mbps. 4K 원본은 GW 로컬 녹화 |
| — (없음) | **3D 모델(13~30 MB)·AI 마커를 30초마다 Gbps급 KOREN 백본으로 배포 — 수백 ms 이내는 전용망만 충족** |

**정말로 수백 Mbps 업링크가 필요하다면 답은 KOREN 백본이 아니라 액세스망 교체다.** 유일한 선택지가 **이음 특화망 5G-A(판교)**다(§3.6 갭 2) — 특화망은 UL/DL 슬롯 비율 조정으로 업링크를 크게 가져갈 수 있고 KOREN에 직결되므로, 판교 AI Network Lab에서 시연하면 "드론이 상용망을 거치지 않고 KOREN에 직접 진입"하는 구성이 성립한다. 이 경우 대조 실험의 명제가 "백본 비교"에서 "**액세스망까지 포함한 전 구간 전용망 vs 상용망**"으로 격상된다. 화상회의 확인 항목 최우선순위로 올린다.

**추가 확인 필요:** 충남대 KOREN 접속 회선의 실제 대역(1G/10G/40G 중 어느 것인지). 위 표의 판정이 여기에 걸려 있는데 현재 미확인이다.

---

## 3. KOREN 진입 — 할 것 2번 (egress, 홀펀칭)

### 3.1 KOREN·시즌13 사실 정리 (2026-08 조사)

**넷 챌린지 캠프 시즌13 (우리가 참가 중인 회차)**
- 주최 과기정통부, 주관 한국지능정보사회진흥원(NIA)·KOREN 연구협력포럼. 주제 "AI 고속도로를 실현할 미래 인재의 도전".
- 일정: 신청 6/1~6/22 → 선정 발표 6/30 → **착수보고회 7월(서류통과 23개 팀, KOREN 이용방법·HPC·GPU 교육 포함)** → **중간평가 8월** → 최종평가 10월 → 시상 11월. 즉 네트워크 실증은 10월 최종평가까지 완성돼야 한다.
- 지원: 학생팀 연구개발비 1차 200만 원 + 2차 200만 원, KOREN 자원(회선·HPC·GPU·시험환경), 넷챌린저 데이(멘토·이용기관 연구자 대상 구현 가속화 지원).
- **전용 문의처: netcc@koren.kr (넷챌린지 사무국)** — 일반 NOC(noc@koren.kr, 02-6191-2081)보다 이쪽이 1차 창구.

**KOREN 망 자체**
- 전국 10개 대도시 PoP(서울·수원·판교·대전·전주·광주·대구·부산·제주·춘천), 백본 10Gbps~7Tbps(2024년 WDM 400G/1T 상향), 사용자 회선 최대 400G 수용.
- HPC 연계 서비스: 200 CPU 코어·Tera급 스토리지·GPU. → Core HPC 후보.
- 국제연구망(TEIN, GEANT, Internet2)과는 100G로 연동. **그러나 상용 인터넷과의 연동(egress/ingress) 정책은 공개 자료에서 확인 불가** — 이것이 이메일로 확인할 핵심.

### 3.2 팀이 KOREN에 접속하는 경로 (시즌13 기준 정리)

넷챌린지 팀 입장에서 KOREN 자원에 손이 닿는 경로는 4가지다. 공개 자료에 원격 접속의 기술적 세부(VPN 방식·계정 발급 절차)는 없으며, **착수보고회 교육 자료가 1차 소스**다 — 자료를 이미 받았으면 아래 표의 ?를 그것으로 채우고, 없으면 netcc@koren.kr로 문의한다.

| 경로 | 무엇을 할 수 있나 | 우리 용도 | 확인 상태 |
|---|---|---|---|
| ① 이용기관(소속 대학) KOREN 회선 | 캠퍼스에서 KOREN 백본 직결. 학교가 가입기관인지는 koren.kr `네트워크 구성 > 이용기관 현황`에서 확인 | **브리지 지점(시나리오 C)이자 실증 거점 후보 1순위** | 학교 가입 여부 확인 필요 |
| ② 원격 접속(VPN/계정) | 외부 인터넷에서 KOREN 내 VM·HPC에 개발용 접속 | 평시 개발·배포 | **방식 미공개 — 착수보고회 자료 또는 netcc 문의** |
| ③ 판교 AI Network Lab 방문 | 시험환경(차폐룸·네트워크 테스트룸), 장비 반입, 공간예약(koren.kr `이용안내 > 공간예약`) | 드론·GW 장비 붙여서 하는 통합 리허설 | 예약제, 031-5182-9175 / ainetworklab@gmail.com |
| ④ NIA PoP 10곳 시험·실증 환경 | 현장 인근 PoP에 Edge VM 배치 | **T4 Edge VM의 물리적 위치** | 시즌13 제공 자원에 명시됨, 배치 절차는 netcc 문의 |
| (참고) 신규 회선 가입 | 기관 명의 신청서 → NOC 심의(최소 1주) → 개통 | 학생팀에게는 과함 — ①~④로 충분 | — |

개발 단계별로 쓰는 경로가 다르다: **평시 개발은 ②**(원격으로 Core HPC에 파이프라인 구축), **대조 실험·실증은 ①**(학교 캠퍼스에서 무선 구간까지 포함해 측정), **최종 리허설은 ③**, **Edge 배포는 ④**.

### 3.3 확인 전이라도 설계가 가능한 이유: 시나리오 3개와 폴백 사다리

이동통신 3사는 5G/LTE 단말에 CGNAT 사설 IP를 준다. 즉 **현장 GW는 인바운드를 절대 못 받고, outbound 연결만 가능**하다. 따라서 어느 시나리오든 "현장이 먼저 접속을 연다"는 방향은 고정이고, 변수는 KOREN 쪽 끝점이 어떻게 인터넷과 만나느냐뿐이다.

| 시나리오 | 조건 (NOC 답변에 따라 확정) | 데이터 경로 | 구현 난이도 |
|---|---|---|---|
| **A. 직접 인바운드** | KOREN VM에 공인 IP + 인터넷 인바운드 허용 | 현장 GW →(SRT)→ KOREN VM 직접 | 최소. NAT 문제 소멸 |
| **B. 홀펀칭** | KOREN에서 인터넷으로 outbound(egress)만 가능 | 양쪽이 밖으로 나와 만남: **SRT rendezvous 모드**(UDP 동시 발신) | 중간. 시그널링용 소형 서버 필요 |
| **C. 브리지/릴레이** | KOREN이 인터넷과 완전 분리 | 인터넷+KOREN 양쪽에 발 걸친 중계 지점 경유: ① 이용기관(학교) 듀얼홈 서버 또는 ② 공인 IP VPS 릴레이 + 학교의 KOREN 회선 | 높음. 그러나 무조건 동작 |

**홀펀칭에 대한 판단 (회의록 "된다면 TCP 홀펀칭 구현하면 됨"에 대한 수정 제안):**
TCP 홀펀칭은 통신사 CGNAT(대칭형에 가까운 매핑) 뒤에서 성공률이 낮고 구현 복잡도가 높다. 반면 우리는 어차피 영상 전송을 **SRT(UDP 기반)**로 확정했고, SRT는 **rendezvous 모드**를 프로토콜 차원에서 지원한다 — 양쪽이 같은 포트로 동시에 발신해 UDP 홀펀칭을 자동 수행한다. 즉 시나리오 B가 되면 TCP 홀펀칭을 새로 구현할 필요 없이 SRT 설정으로 해결된다. **결론: "TCP 홀펀칭 구현" 대신 "SRT rendezvous + 실패 시 시나리오 C 릴레이 폴백"을 권장.**

시나리오 C도 비관 시나리오가 아니라 넷챌린지의 전통적 구성이다: 이용기관(학교)이 KOREN 회선과 일반 인터넷을 둘 다 갖는 유일한 지점이므로, 학교에 둔 서버 1대가 자연스러운 브리지가 된다. 이 경우 실험군 경로는 `현장 5G → 인터넷 → 학교 브리지 → KOREN → Core HPC`, 대조군은 `현장 5G → 인터넷 → Core HPC(일반망 경유)`로 구성되어 대조 실험 프레임도 그대로 유지된다.

### 3.4 넷챌린지 사무국 발송용 이메일 질문 초안

> 제목: [넷챌린지 시즌13] KOREN 자원 네트워크 연동 문의 (팀 SkyLens)
>
> 안녕하세요, 넷챌린지 캠프 시즌13 참가팀 SkyLens입니다. 재난 현장의 드론 영상을 상용 5G로 수집해 KOREN 내 GPU 자원에서 3D 복원하는 과제를 준비 중이며, 망 연동 관련해 아래를 문의드립니다.
>
> 1. 팀에 제공되는 KOREN 내 VM/HPC 자원에서 **상용 인터넷으로의 아웃바운드(egress)** 통신이 가능한가요? 가능하다면 프로토콜/포트 제한이 있나요? (UDP 사용 예정입니다 — SRT 스트리밍)
> 2. 해당 자원에 **공인 IP를 할당받아 인터넷에서 인바운드 접속**을 받는 것이 가능한가요? (상용 5G망의 드론 게이트웨이가 접속하는 용도)
> 3. 불가할 경우, 이용기관(소속 대학) KOREN 회선을 경유해 인터넷 트래픽을 KOREN 내부 자원으로 중계하는 구성에 정책적 제약이 있나요?
> 4. 상용 5G/LTE망과 KOREN 간 연동을 지원받은 과거 사례(시즌7 연세대 스마트팩토리 등)와 같은 지원이 이번 시즌에도 가능한가요?
> 5. 원격 개발을 위한 KOREN 자원 접속 방법(VPN 등)과, 팀에 제공되는 VM/HPC의 네트워크 위치(어느 PoP인지)를 안내 부탁드립니다.
> 6. Edge VM을 특정 NIA PoP(실증 지역 인근)에 배치하는 것이 가능한지, 절차를 안내 부탁드립니다.
>
> 수신: netcc@koren.kr (참조: noc@koren.kr)

답변 결과에 따라 §3.3 표에서 시나리오를 하나 지우고 확정하고, §3.2 표의 미확인 칸을 채운다.

### 3.5 사무국 회신(2026-08-04)과 확정된 구조

**회신 요지 (netcc@koren.kr):**
1. KOREN 망 접속에는 **반드시 고정 IP가 필요**하다.
2. 드론을 망외에서 운영하는 경우의 일반적 활용법: **에지 서버(PC 등)를 망외에 두고, 드론↔에지 통신 후 에지가 KOREN 망에 접속**한다.
3. 모든 KOREN 자원은 망내에 있다 (예외는 자원별 엔지니어 확인 필요).
4. 망외에서 KOREN으로 통신하려면 고정 IP를 만들어야 한다.

**해석:**
- 회신의 "망외 에지 서버 + 드론↔에지 + 에지→KOREN" 구조는 **우리 현장 게이트웨이 설계와 정확히 일치**한다. 아키텍처 자체는 검증받은 셈.
- 시나리오 A/B/C 프레임은 이렇게 정리된다: KOREN은 임의 IP의 인바운드를 받지 않지만(A 원형 탈락), **고정 IP를 등록한 망외 지점의 접속은 허용**한다. 즉 확정 구조는 "**A' = 고정 IP 등록 기반 직접 접속**"이고, 접속 자격이 '공인 IP 여부'가 아니라 '등록된 고정 IP 여부'로 판명된 것.
- **시나리오 B(홀펀칭)는 폐기.** CGNAT 뒤에서 몰래 만날 필요 없이 공식 경로(고정 IP 등록)가 존재하므로 구현할 이유가 없다.
- 시나리오 C(충남대 브리지)는 폐기가 아니라 **A'의 구현 방법 중 하나**로 흡수된다 — 학교는 고정 IP를 이미 보유한 지점이기 때문.

**확정 아키텍처의 남은 선택지: 고정 IP를 어디에 두느냐**

| | 방법 1: 현장 GW에 고정 IP 부여 | 방법 2: 충남대 브리지 경유 |
|---|---|---|
| 구성 | 통신사 기업용 고정 IP 회선(예: LG U+ 프리미엄 M2M 5G 라우터 — 건설현장 등 유선 불가 지역용 상품 실재)을 현장 GW에 장착 → GW가 곧 KOREN 접속 자격 보유 | 현장 GW는 일반 5G(CGNAT) → 충남대의 고정 IP 서버로 송신 → 그 서버가 KOREN 접속 |
| 장점 | 홉 최소화, "재난 현장에서 직접 KOREN 진입" 서사가 깔끔 | 추가 비용 없음(학교 IP 활용), 지금 당장 구축 가능 |
| 단점 | 기업용 요금제 가입 필요(약정·비용 확인 필요), 개통 리드타임 | 홉 1개 추가(지연 소폭 증가), 학교 방화벽/네트워크팀 협조 필요 |
| 판단 | 최종평가 데모용 본선 구성 후보 | 개발·중간 단계 기본 구성, 방법 1의 폴백 |

**권장: 방법 2로 즉시 시작하고, 방법 1은 비용·개통 확인 후 최종평가용으로 승격 검토.** 두 방법은 GW 이후 파이프라인이 동일하므로 나중에 갈아타도 코드 변경이 없다.

**추가 확정 사실 (사무국·자체 확인):**
- 접속 방식 = **우리 측 고정 IP를 KOREN에 등록(허용)하는 방식** (사무국 확인). 별도 회선/VPN 개통이 아니므로, 고정 IP만 확보하면 등록 요청 한 번으로 접속 자격이 생긴다.
- **충남대는 KOREN 이용기관** (확인 완료). 방법 2의 전제 성립 — 학교에서 고정 IP 서버만 확보하면 된다.

### 3.6 자원 신청 구글폼으로 확인된 사실 (2026-08-05)

팀이 제출한 KOREN 자원 신청 구글폼에서 2차 문의 대상이던 사항 대부분이 해소됐다.

**절차 관련**
- 자원 신청은 구글폼으로 진행. 서비스 유형: ① 자원 신규 신청(1회 한정) ② 자원 변경·반납 ③ **방화벽 개방 등 네트워크 기술 지원**. 최초 신청 전 NOC(netcc@koren.kr)와 사전 화상회의 필수.
- **IP 등록 = 폼 7번** ("상용망(KT, SKB, LGU+ 등)에서 KOREN 자원 접근하고자 하는 장비의 공인 IP" — KOREN은 폐쇄망이므로 사전 등록 IP 외 접속 불가). 상용망 접근을 전제한 문항이므로 통신사 고정 IP(M2M 라우터)도 등록 가능할 것으로 보임(화상회의에서 확답 필요).
- **방화벽 개방 = 폼 8번** (소스IP:포트 → 목적지IP:포트 형식으로 신청, 추후 수정 가능).

**자원 카탈로그와 위치**
- KOREN AI Cloud GPU(H200) / AI Cloud POD / **HPC V100(판교)** / **KOREN VM(대전)** / 양자암호 테스트베드 / **이음 특화망 5G-A(판교)** / 차폐실(판교) / 판교 AI Network Lab 서버 전진배치·시험장소.
- 참가자 노트북은 **VPN 접속**으로 KOREN 망내 진입 가능(폼 예시에 명시) — 원격 개발 경로 확인됨.

**팀 신청 현황 (제출분)**
- HW 구성: HPC V100(판교) – KOREN VM(대전) – 연구실 PC(충남대 공대 5호관) 및 개인 노트북(VPN).
- 등록 IP: 168.188.128.36(충남대 서버), 175.126.98.44(자택).
- 방화벽 신청분: 충남대→VM :22, 자택→VM :22, VM→외부 :80,443(egress — 패키지·데이터셋 다운로드용).

**이에 따른 Core 배치 구체화**: MediaMTX 수신 + FastAPI/Redis 오케스트레이터 = **KOREN VM(대전)**, GPU 잡(3DGS·UNet) = **HPC V100(판교) 또는 AI Cloud GPU(H200)**. 대전 VM↔판교 HPC 구간은 KOREN 백본이므로 프레임/산출물 이동 비용은 무시 가능. 충남대→대전은 지리적 최단.

**발견된 갭 (즉시 조치 필요)**
1. **방화벽 신청분에 영상 스트림 포트가 없다.** 현재 22/80/443뿐이라 이대로면 SRT가 차단된다. 폼 8번 수정으로 추가할 것: `소스 168.188.128.36:ALL → 목적지 [KOREN VM]:UDP 8890` (+ 향후 M2M 라우터 IP 확정 시 그 IP도 동일하게).
2. **이음 특화망 5G-A(판교)가 최종평가 시연의 제3 옵션.** 판교 AI Network Lab에서 시연한다면 드론 GW를 이음 5G에 붙여 M2M 라우터 없이 "5G→KOREN 직결" 구성이 가능. 방법 1(M2M)과 비교 검토할 것.
3. **대조군 경로 재설계 필요.** KOREN이 폐쇄망(등록 IP만 접속)이므로 "같은 VM에 일반 인터넷으로도 접속"하는 대조는 성립하지 않을 수 있다. 현실적 대조군은 **상용 클라우드 VM+GPU(일반 인터넷 경유)에 동일 파이프라인을 배포**하는 것 — "같은 코드, 두 경로" 원칙에도 부합. 설계 확정 필요.

**남은 미확정 (사전 화상회의 또는 메일에서 확인)**
- 방화벽 개방에서 **UDP(8890, SRT) 허용 가능 여부** — 절차는 있으나 UDP 허용 전례 확인 필요.
- **M2M 라우터 무선 고정 IP의 등록 가능 여부** 확답.
- AI Cloud GPU(H200)와 HPC V100 중 우리 워크로드(3DGS 학습) 배정 협의.

### 3.7 [설계 오류] Edge VM 배치 — egress가 아니라 ingress가 문제다

§1 구조도와 NETWORK.excalidraw는 `Edge VM(NIA PoP) → 일반 인터넷 → 상황판·대원 단말·심사위원`으로 그려져 있고 "Edge VM에서 나가는 순간 KOREN은 끝난다"고 주석했다. **이 구간이 성립하지 않는다.** 업링크 병목(§2.5)보다 이쪽이 데모를 직접 무산시킨다.

**왜 깨지는가**

단말이 뷰어와 `.ply`를 받는 것은 단말이 Edge VM에 HTTPS로 **접속을 여는** 행위다. 즉 KOREN 입장에서는 egress가 아니라 **인바운드**다. 그런데
- NOC 회신: KOREN 접속에는 반드시 고정 IP가 필요하고, 모든 KOREN 자원은 망내에 있다.
- 폼 7번: "상용망에서 KOREN 자원 접근하고자 하는 장비의 공인 IP"를 사전 등록해야 하며 등록 IP 외 접속 불가.

현장 대원 단말은 상용 5G의 CGNAT 동적 IP다 — **등록할 IP가 존재하지 않는다.** 관제탑 노트북과 최종평가장 심사위원 단말도 그 장소의 공인 IP를 사전에 알아 등록해야 하는데, 발표장 IP는 우리가 통제할 수 없다.

**egress 자체는 별개이고, 가능성이 높다**

NOC 회신은 1번 질문(KOREN → 상용 인터넷 아웃바운드)에 답하지 않고 인바운드 정책만 답했다. 그러나 자원 신청 폼 8번이 소스IP:포트 → 목적지IP:포트 형식의 방화벽 신청을 받고 있고, 팀이 이미 `VM → 외부 :80,443`(패키지·데이터셋 다운로드용)을 신청했다. **egress는 정책적으로 존재하며 목적지·포트 화이트리스트 방식**으로 보는 것이 합리적이다. 미확인은 두 가지: 임의 목적지 egress가 되는지(아니면 등록 목적지만인지), UDP egress가 되는지.

**해결안 3개**

| | A. 단말을 등록 IP 뒤로 | **B. Edge를 충남대로 (권장)** | C. NIA PoP 예외 승인 |
|---|---|---|---|
| 방법 | 대원 단말은 GW WiFi AP → 고정 IP M2M 라우터 → KOREN. 관제탑은 그 장소 공인 IP 등록 | KOREN VM이 egress로 충남대 168.188.128.36에 모델 push, 학교 서버가 공개 HTTPS 서빙 | NIA PoP Edge VM에 공인 IP + 임의 인바운드 허용을 예외로 요청 |
| 성립 조건 | M2M 고정 IP 등록 가능(미확인) | **egress 허용(근거 있음)** + 학교 서버 공개 서빙 | NOC 회신의 "예외사항은 전문 엔지니어 확인 필요"에 걸어 봄 |
| 약점 | 발표장 IP를 사전에 알아야 함 — 통제 불가 | Edge가 대전 고정, PoP 근접 배치 이점 상실 | 승인 여부를 우리가 통제 못 함 |

**권장: B를 기본값으로 확정하고, C는 화상회의에서 확인 후 되면 승격, A는 현장 대원용 보조 경로로 유지.**

B의 논거는 충남대가 **KOREN 이용기관이면서 공인 IP를 가진 유일한 듀얼홈 지점**이라는 것이다. 업링크 진입점과 다운링크 배포점을 같은 노드가 맡으면 등록 IP 문제가 한 지점에서 전부 해소된다. 판교→충남대 구간이 KOREN 백본이므로 §2.5의 "모델을 Gbps급 전용망으로 저지연 배포" 서사도 그대로 유지된다. 실증 장소를 학교 캠퍼스로 하면(액션 6번 권장안) 지리적 근접성까지 확보되어 PoP 배치 이점 상실도 상쇄된다.

**수정된 데이터 경로 (B 기준)**

```
현장 GW ─(5G/인터넷)→ 충남대 168.188.128.36 ─(KOREN)→ 대전 VM ─(KOREN)→ 판교 GPU
                            ↑                                              │
                            └───────────(KOREN 백본, 모델 push)────────────┘
                            │
                            └─(일반 인터넷/5G)→ 상황판 · 대원 단말 · 심사위원
```

충남대 브리지가 관문(ingress)이자 배포점(Edge)을 겸하는 대칭 구조가 된다. §1 구조도와 NETWORK.excalidraw의 Edge VM 블록을 이에 맞춰 수정해야 한다.

**이에 따라 추가되는 방화벽 신청분 (폼 8번)**
- `KOREN VM(대전) → 168.188.128.36 : TCP 443` (모델 push egress) — 갭 1의 UDP 8890과 함께 신청.

---

## 4. 통신 스펙 — 할 것 4번 (nginx·미들웨어·프로토콜)

### 4.1 구간별 프로토콜 표

| 구간 | 프로토콜 | 포트(기본) | 근거 |
|---|---|---|---|
| 드론 → GW | 제조사 링크 / RTSP | — | 옵션 A 기준 |
| GW → Core (영상+열화상) | **SRT** (caller 또는 rendezvous) | UDP 8890 (MediaMTX) | UDP 기반 저지연 + ARQ, 손실망 내성, rendezvous로 NAT 통과. RTMP는 지연 10초+라 탈락, WebRTC는 서버 수신 파이프라인이 SRT보다 복잡 |
| GW → Core (포즈·관제) | **WebSocket(WSS)** + MAVLink 페이로드 | TCP 443 | 기존 결정(WebSocket 군집 관제) 유지, 443 단일 포트로 방화벽 친화 |
| Core 내부 | MediaMTX → 파이프라인, FastAPI+Redis | 내부 | ARCHITECTURE.md 확정 스택 |
| Core → Edge (복원본 push) | HTTPS PUT (오브젝트 스토리지 동기화) | TCP 443 | 30초마다 13~30 MB — KOREN 백본에서 1초 미만 |
| Edge → 단말 (3D 뷰어) | HTTPS 정적 `.ply`/`.spz` + Range 요청 | TCP 443 | 뷰어는 파일 단위 로드(현 구현). LOD/청크는 `.spz` 전환 시 확장 |
| Edge → 단말 (마커·이벤트) | WSS push | TCP 443 | AI가 "발견 시마다 알리는" 채널. 30초 주기와 독립 |
| 시그널링(시나리오 B) | HTTPS 소형 API (자체 호스팅) | TCP 443 | 현 프로토타입의 공개 PeerJS 브로커·구글 STUN 의존은 실증에서 자체 호스팅으로 교체 |

### 4.2 nginx의 위치 — "미디어는 MediaMTX, 웹은 nginx"

회의에서 "앞단에 nginx"가 언급됐지만 기존 문서에는 nginx가 없다(스트림 수신을 MediaMTX로 확정했기 때문). 둘은 경쟁 관계가 아니라 역할이 다르다:

- **MediaMTX (Core HPC 앞단)**: SRT/RTSP/WebRTC 스트림 수신 전용. 영상 경로에는 nginx를 두지 않는다 — UDP(SRT)는 nginx가 프록시할 수 없고, 미디어 경로에 홉을 추가할 이유가 없다.
- **nginx (Edge VM 앞단)**: TLS 종단, 정적 파일(`.ply`/`.spz`, 뷰어 번들) 서빙 + 캐시, WSS 리버스 프록시, FastAPI 리버스 프록시. 단말이 보는 모든 것이 443 하나로 수렴하므로 재난 현장 단말의 방화벽/프록시 환경에서도 안전하다.
- **(시나리오 C일 때) 브리지 서버**: nginx `stream` 모듈(L4)로 TCP 중계 + UDP는 iptables DNAT 또는 srt-live-transmit 릴레이.

### 4.3 30초 예산 분배 (버전 1 기준 목표)

| 단계 | 예산 | 비고 |
|---|---|---|
| 드론→GW→Core 업링크 (스트림 지연) | 1~2 s | SRT latency 설정 200~500 ms + 인코딩 |
| 세그먼트 확정 대기 (영상 슬라이스) | ~10 s | 10초 분량 프레임으로 잡 1회 구성 |
| 3DGS 증분 복원 잡 1회 | 10~15 s | 포즈 선공급으로 SfM 생략 전제. **HPC 실측으로 확정 필요** — 로컬 실측은 학습만 3.2분이므로 iteration 축소+GPU로 달성해야 함 |
| Core → Edge push | < 1 s | 30 MB, KOREN 백본 |
| Edge → 단말 로드(TTFV) | 1~3 s | 13~30 MB ÷ 단말 다운링크 |
| **합계** | **≈ 23~31 s** | 이후 주기부터는 파이프라인이 겹쳐 30초마다 갱신 |

이 표의 복원 잡 항목이 전체 성립 여부를 쥐고 있다. 네트워크 쪽 예산(업링크+push+TTFV)은 5초 내외로 여유가 있으므로, **HPC GPU에서 증분 복원 1회가 15초 안에 끝나는지가 다음 실측 과제**다(가우시안 병합 가능 여부와 함께 — 회의록 "할 것 1번"과 맞물림).

### 4.4 계측 지점 (ARCHITECTURE.md §4 빈칸 채우기)

대조 측정표의 4개 지표를 이 아키텍처의 구체 지점에 매핑한다. 모든 노드는 NTP 동기화(GW는 GPS 시각 사용 가능).

| 지표 | 측정 지점 | 방법 |
|---|---|---|
| 다중 4K 스트림 손실/지연 | GW 송신 ↔ MediaMTX 수신 | SRT 통계 API(RTT, 재전송률, drop), 경로별(KOREN/일반망) 동시 수집 |
| 멀티드론→3D 갱신 E2E 지연 | 드론 촬영 타임스탬프 ↔ Edge 반영 시각 | 프레임에 박은 타임코드가 복원본 메타데이터로 전파 |
| 복원+추론 시간 | 오케스트레이터 잡 로그 | FastAPI 잡 시작/종료 이벤트 |
| 첫 3D 표시 시간(TTFV) | 단말 브라우저 | Navigation Timing + 뷰어 로드 완료 이벤트 |

---

## 5. 결정 필요 / 액션

~~0. 착수보고회 자료 확인~~ / ~~1. 사무국 1차 이메일~~ → **완료. 회신 수신(2026-08-04), §3.5로 구조 확정. 자원 신청 폼으로 절차·위치 확인(§3.6).**

1. **[수정] 자원 신청 폼 8번 방화벽 항목** — 두 건 추가. ① SRT용 `충남대 서버 → KOREN VM : UDP 8890` (§3.6 갭 1) ② 모델 배포용 `KOREN VM → 168.188.128.36 : TCP 443` egress (§3.7). 현재 신청분(22/80/443)만으로는 영상도 배포도 차단됨.
2. **[확인] 사전 화상회의/메일에서** — UDP 개방 가능 여부, M2M 무선 고정 IP 등록 확답, GPU 워크로드 배정(H200 vs V100), **이음 5G-A(판교) 시연 활용 가능성(§2.5 — 업링크 병목을 없애는 유일한 선택지라 우선순위 최상)**, **NIA PoP Edge VM의 임의 인바운드 예외 가능 여부(§3.7 안 C)**, **충남대 KOREN 접속 회선의 실제 대역(1G/10G/40G)**.
3. **[설계] 대조군 경로 확정** — 상용 클라우드 VM+GPU에 동일 파이프라인 배포하는 안 검토. (§3.6 갭 3) 측정 명제는 "영상 업링크 대역"이 아니라 **"모델 배포 지연 + GPU 자원 접근"**으로 재정의됨(§2.5) — 업링크는 양 군 공통 변수.
3-1. **[수정] 제안서·발표자료의 대역폭 주장** — "드론당 50~100Mbps / 수백 Mbps 동시 전송 / 영상을 10Gbps 고속망으로"를 §2.5 정정 표대로 교체. 현 문구는 액세스망 병목 지적에 그대로 무너짐.
3-2. **[수정] NETWORK.excalidraw** — Edge VM 블록을 NIA PoP → 충남대 브리지 겸용으로 변경하고, "Edge VM에서 나가는 순간 KOREN은 끝난다" 주석을 §3.7 내용으로 교체.
4. **[조사] 통신사 기업용 고정 IP 무선 회선** (방법 1용) — LG U+ 프리미엄 M2M 5G 라우터 등 요금·약정·개통 리드타임. 이음 5G-A(판교) 옵션과 비교 후 9월 초까지 결정.
5. **[실측] HPC에서 증분 복원 잡 1회 소요 시간** — 30초 예산표의 최대 리스크 항목.
6. **[결정] 실증 장소** — 대조 실험은 무선 구간이 공통이어야 공정하므로, 실증일에 GW를 어디 두고 5G 품질을 어떻게 통제할지(학교 캠퍼스 권장) 팀 합의 필요.
7. **[교체] 프로토타입의 공개 PeerJS 브로커·구글 STUN** → Edge VM의 자체 시그널링/WSS로 이관 (§4.1 마지막 행).
8. **[확인] 발표장 온라인 여부** — Google 3D Tiles 온라인 필수 제약(RESEARCH-GEO3D.md §10)과 묶어서 확인.

